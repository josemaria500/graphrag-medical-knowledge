# =====================================================
# GraphRAG Medical Knowledge - dependencias mínimas
# Frontend = HTML/JS/Cytoscape.js (servido por FastAPI)
# =====================================================

# --- API web + servidor + plantillas ---
fastapi==0.141.1
uvicorn[standard]==0.52.1
jinja2==3.1.6

# --- Base de datos de grafos (versión estable compatible con Aura) ---
neo4j==4.4.12

# --- LLM (GPT-4o-mini) ---
openai==2.45.0

# --- API de ClinicalTrials.gov ---
requests==2.34.2

# --- Configuración (.env) ---
python-dotenv==1.2.2
